mylib: main.c print.c print.h
	gcc -o mylib main.c print.c -I.

clean:
	rm -rf *.o *~ mylib

# try to modify a .c file and recompile the library

# try to modify the .h file without changing any .c file and recompile
# the library
